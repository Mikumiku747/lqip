/*
 * encode.h
 * Developer: 
 * Student #: 
 * Generates Base64 encoded versions of the images after resizing.  
 * 
 * Main tasks:
 * 	
 * 	Image encoder: Takes a small pixel array format image and turns it into 
 * 		a native image format which the browser can read. Then outputs that
 * 		as a Base64 character string. 
*/

/* Include Guard. */
#ifndef ENCODE_H
#define ENCODE_H

void encodeImage();

#endif